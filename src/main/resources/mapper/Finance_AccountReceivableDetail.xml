<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code.dao.Finance_AccountReceivableDetail">
    <select id="getAccountReceivableDetail">
    select  * from  account_receivable_detail where invoices_date between  #{date1} and #{date2} and clientele_id=#{clientele_id}
    </select>
</mapper>