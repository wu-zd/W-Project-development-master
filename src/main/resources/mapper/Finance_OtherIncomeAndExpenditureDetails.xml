<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code.dao.Finance_OtherIncomeAndExpenditureDetails">
    <select id="getOtherIncomeAndExpenditureDetails">
        select * from other_income_and_expenditure_details where `date` between  #{date1} and #{date2} and remark=#{remark} and Income_category=#{incomeCategory}
    </select>
</mapper>